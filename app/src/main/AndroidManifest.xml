<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.hritik.resume">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Display"></activity>
        <activity android:name=".Selection" />
        <activity android:name=".Exp_Desc" />
        <activity android:name=".Int_Desc" />
        <activity android:name=".Edu_Desc" />
        <activity android:name=".Prg_Desc" />
        <activity android:name=".Ach_Desc" />
        <activity android:name=".MainResume" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>